{
  "name": "@ohanaui/merge",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "pnpm run vitest dev",
    "test": "pnpm run vitest run",
    "tsc": "tsc",
    "tsc:no-emit": "pnpm run tsc --noEmit",
    "typecheck": "pnpm run tsc:no-emit",
    "vitest": "vitest --config ../../@tools/vitest/globals/config"
  },
  "devDependencies": {
    "@tools/typescript": "workspace:*",
    "@tools/vitest": "workspace:*"
  }
}
